apiVersion: v1
kind: Service

metadata:
  name: {{ .Release.Name }}

spec:
  type: {{ .Values.service.type }}

  selector:
    app: payment-service

  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}